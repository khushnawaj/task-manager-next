# Task Manager Client (Next.js)

## Run
1. `cd client && npm install`
2. Set env in `.env.local` if needed:
   - NEXT_PUBLIC_API=http://localhost:4000
   - NEXT_PUBLIC_SOCKET=http://localhost:4000
3. `npm run dev`
